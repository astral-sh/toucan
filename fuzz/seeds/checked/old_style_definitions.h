int bound(int);
int (*factory(a))(int result) unsigned char a; { return 0; }
int process(n, values, callback)
  int callback(int);
  int n;
  int (*values)[n];
{
  return callback((*values)[0]) + __builtin_constant_p(sizeof *values);
}
int f(a, b)
  float b;
  unsigned char a;
{
  return a + (int)b;
}
