# Integrated omitted-middle conditionals

The root integration passes all 12 new parser, semantic and facade tests, including native C probes and current Rust calls. Separate GCC and Clang runs each execute generated bindings with actual Rust 1.64 in all eight modes at C O0/O2. Workspace Clippy passes. `summary.json` identifies the parent commit and changed source; compressed logs preserve these results.

The adjacent original layer retains complete native probe records, project audits, benchmarks and sanitizer evidence. The following combined gates cover the later stack composition.
