[package]
name = "toucan-aws-lc-consumer"
version = "0.0.0"
edition = "2021"
rust-version = "1.71"
publish = false

[workspace]

[dependencies]
aws-lc-rs = { version = "=1.18.0", default-features = false, features = ["fips", "bindgen"] }
aws-lc-sys = { package = "aws-lc-fips-sys", version = "=0.14.2", default-features = false, features = ["fips", "bindgen"] }
