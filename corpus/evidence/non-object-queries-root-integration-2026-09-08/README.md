# Integrated void and function queries

This evidence covers the non-object query layer on the current stack, including the shared type-resolution refinement. `summary.json` records the parent commit, changed source hashes, native and Rust 1.64 checks, and all 88 checked seed settings. The replay compares ordinary and retained translation units or diagnostics and checks graph invariants. Logs and the original devbox replay source are preserved.

The adjacent frozen layer records its cross-target and native compiler probes. Combined workspace, sanitizer and non-Linux checks are separate gates.
